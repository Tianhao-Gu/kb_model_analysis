#
# define display information
#
name: Model Comparison

tooltip: |
    Retrieve statistical data from models and visualize the comparison via a heatmap

screenshots: []

icon: fba-teal.png

#
# define a set of similar apps that might be useful to the user
#
suggestions:
    apps:
        related:
            []
        next:
            []

#
# Configure the display and description of parameters
#
parameters :
    # staging_file_path :
    #     ui-name : |
    #         Model Comparison File Path
    #     short-hint : |
    #         Recognized extensions for the input file is .xlsx or .csv
    #     long-hint  : |
    #         Recognized extensions for the input file is .xlsx or .csv

    attri_mapping_ref :
        ui-name : |
            Model Set Object
        short-hint : |
            Model Set Object
        long-hint  : |
            Model Set Object

description : |
    <p>Retrieve statistical data from models and visualize the comparison via a heatmap</p>
